const carName = "Audi";
let maxCarSpeed = 320;
let carOwner = "Renat";

alert(`carName: ${carName}`);
alert(`maxCarSpeed: ${maxCarSpeed}`);
alert(`carOwner: ${carOwner}`);
alert(`"У машины изменилась скорость:" ${maxCarSpeed}`);
maxCarSpeed = 280;
